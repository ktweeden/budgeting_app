<main>
  <form action="/budgets/update/<%=@budget.id%>" method="post">
    <label for="tag">Tag:</label>
    <select name="tag_id">
      <% @tags.each do |tag| %>
        <option value="<%=tag.id%>" <% if @budget.tag_id == tag.id %>selected="selected"<% end %>>
          <%=tag.name%>
        </option>
      <% end %>
    </select>
    <label for="amount">Amount:</label>
    <input type="number" name="amount" value="<%= to_pounds(@budget.amount)%>"/>
    <input type="submit" value="Update Budget" />
  </form>
</main>
